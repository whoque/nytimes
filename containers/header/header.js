import styles from './header.module.scss';

const Header = () => {
    return (
        <div className={styles.wrapper}>
            <span>THE NEWYORK TIMES</span>
        </div>
    )
}

export default Header;